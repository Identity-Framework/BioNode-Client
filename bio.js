var path = require('path');
var fs = require('fs');
var acl = require('acl');
var file_path = process.argv[1];
var file = process.argv[2];

fs.readFile(file_path, function(err, contents){
	var file_content = contents.toString();
}

var biometric.prototype.reason = function(file_content){
}
